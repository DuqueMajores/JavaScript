// Undefined significqa que uma variável foi declarada, mas não tem valor atribuído.

let x;
console.log(x); // undefined

// É o valor padrão de variáveis não inicializadas. Também é retornado por funções que não têm return.

function teste(){
    console.log(teste()); // undefined
}
