// Representa um valor lógico, true ou false.

let ativo = true;
let aprovado = false;

// Usados comunmente em estruturas condicionais.

if (ativo) console.log("Usuário está ativo");

// Operações de comparação retornam booleanos.

console.log(5 > 2); // true
console.log("a" === "b"); // false
